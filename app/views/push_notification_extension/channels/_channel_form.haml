= simple_form_for(@channel, :html => {class: 'form-horizontal'}) do |f|
  .form-inputs
    = f.input :name
  .form-actions
    = f.button :submit, {class: 'btn btn-primary'}
    = link_to 'Cancel', settings_path, class: 'btn btn-link'
